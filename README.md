# Palindrome_Checker
The Palindrome Checker is a simple JavaScript project that determines whether a given word, phrase, or number reads the same forwards and backwards. It takes user input, processes it by removing spaces, punctuation, and making it case-insensitive, then checks if the cleaned string matches its reverse.
